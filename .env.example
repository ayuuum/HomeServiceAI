# Supabase (フロントエンド用)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key

# 本番の公開URL（LINE LIFF やメールリンクで使用。未設定時は window.location.origin）
# VITE_PUBLIC_URL=https://your-domain.com

# オプション: デフォルト組織スラッグ
# VITE_DEFAULT_ORG_SLUG=haukuri

# ---
# AI 機能（LINE AI・予約アシスタント・管理画面 AI）を使う場合、
# Supabase Dashboard > Edge Functions > Secrets に以下を設定してください。
# この .env には含めません（Edge Functions はサーバー側で実行されます）。
#
# OPENAI_API_KEY=sk-...
# OPENAI_CHAT_MODEL=gpt-4o-mini   # 未設定時は gpt-4o-mini を使用
